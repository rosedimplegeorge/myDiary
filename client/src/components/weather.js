import React, { Component } from 'react';
import { Button } from 'react-bootstrap';
import {Grid, Row} from 'react-bootstrap';


class Weather extends Component {
    render(){
        return(
            <div><Button>Weather Info</Button></div>
        )
    }
}

export default Weather